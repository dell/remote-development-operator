apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {name}
  labels:
    app: {name}
spec:
  storageClassName: efs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {size}
